learning_log = project
learning_logs = app
ll_env = virtual env

models.py = a class to define the data we want to manage in our app.

Create a model or modify models, run the command py manage.py makemigrations 
app_name, and then run the command py manage.py migrate. Finally, register 
the model in admin.py.

To make a web page... define an URL, write a view, write a template.

To allow a user to enter and submit information to any page on a webpage, you 
must create a form.  Then, define a URL, write a view function and a template, 
and link to the page.

https://git.heroku.com/radiant-shelf-14366.git